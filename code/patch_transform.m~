
img_size = [m, n];
R = zeros(p*p, m*n, num_patches);
num_patches = (m - p + 1)*(n - p + 1);

for r = 1: (m - p + 1)
    for c = 1: (n - p + 1)
        
        npatch = (c - 1) * (m - p + 1) + r;
        
        R_1 = zeros(p*p, m*n);
        for i = 1:num_patches
            for j = 1:num_patches
                row = (j - 1)*num_patches + i;
                col = (r + i - 1)*
            end
        end
        
    end
end